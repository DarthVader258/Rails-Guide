<%#= flash[:notice] %>
<h1>Listing Plan</h1>
<div class="col-lg-12">
	<%= link_to "New Plan", new_plan_path, class: "btn btn-primary btn-lg" %>
</div>

<!--<%# @plans.each do |plan| %>
	<p class="lead">Name: <%#= plan.name %></p>
	<p class="lead">Price: <%#= plan.price %></p>
	<p class="lead">User_id: <%#= plan.user_id %></p>
<%# end %>-->


<div class="row row-cols-1 row-cols-md-2 g-4">
	<% @plans.each do |plan| %>
		<div class="col">
		  <div class="card">
		    <div class="card-body">
		      <h5 class="card-title">Plans</h5>
		      	<p class="lead">Name: <%= plan.name %></p>
		      	<p class="lead">Price: <%= plan.price %></p>
		      	<p class="lead"><%= link_to(plan.active? ? "Activated Plan" : "Get Plan", activate_plan_for_user_plan_path(plan), method: :post, class: "btn btn-primary") %></p>
		    </div>
		  </div>
		</div>
	<% end %>
</div>

