<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%# <a class="navbar-brand" href="#">Rails Guide</a> %>
    <%= link_to "Rails Guide", root_path, class: "navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Question", questions_path, class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Plan", plans_path, class: "nav-link active" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle active" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Settings
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="javascript:void(0)">Signed in as <%= current_user.email %></a></li>
              <li><%= link_to "Edit User", edit_user_registration_path, class: "dropdown-item" %></li>
              <li><%= link_to "Sign Out", destroy_user_session_path, class: "dropdown-item", method: :delete, data:  { confirm: "Are you sure"}%></li>
            </ul>
          </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Sign In", new_user_session_path, class: "nav-link active" %>
            </li>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <%= render "shared/search" %>
      </form>
    </div>
  </div>
</nav>

